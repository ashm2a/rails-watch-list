
<div class = "container" >
<h2> Comment : </h2>
  <h2> <%=  @list.name %></h2>
    <% @list.bookmarks.each do |bookmark|%>
      <%= bookmark.comment %>
      <%=bookmark.movie.title %>
      <%= link_to "supp", bookmark_path(bookmark), data: {turbo_method: :delete, turbo_confirm: "Are you sure ?"}%>
    <%end %>
</div>
<div class="container">
  <%= link_to "Add bookmark", new_list_bookmark_path(@list), class: 'btn btn-primary' %>
  <%= link_to "retour a list", lists_path(@lists), class: 'btn btn-secondary' %>
</div>
